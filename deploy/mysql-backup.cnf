# ============================================================
# MySQL 备份专用凭证文件（权限 600，仅 root 可读）
# ============================================================
# 安装：
#   sudo cp deploy/mysql-backup.cnf /etc/mysql/backup.cnf
#   sudo chmod 600 /etc/mysql/backup.cnf
#   sudo chown root:root /etc/mysql/backup.cnf
# ============================================================

[client]
user = ai_chat_app
password = 替换为真实密码
host = localhost

# ============================================
# 开发环境变量配置
# ============================================
# 作用：定义开发环境下的环境变量。
#       当运行 npm run dev 时，Vite 会自动加载这个文件。
#
# 命名规则：Vite 只暴露以 VITE_ 开头的变量到前端代码中。
#           所以自定义变量必须以 VITE_ 开头。
# ============================================

# API 基础地址（开发环境指向本地后端）
# 后端 SpringBoot 默认运行在 8080 端口
VITE_API_BASE_URL=http://localhost:8080/api
